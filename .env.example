# "Required" Your Anthropic API key (get from https://console.anthropic.com/settings/keys)
# This is required for Fallback API-key based Authentication (SDK, etc.)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ==============================================================================
# OPTIONAL: Arize AI Observability
# ==============================================================================

# --- Observability with Arize AI ---
# Create an account at https://arize.com/
ARIZE_API_KEY=your-api-key-here
ARIZE_SPACE_KEY=your-space-key-here
ARIZE_MODEL_ID=litellm-proxy
ARIZE_MODEL_VERSION=local-dev

# ==============================================================================
# OPTIONAL: Advanced Features (uncomment to enable)
# ==============================================================================

# --- Authentication & Web UI ---
# Uncomment these to enable authentication and web UI access
# Note: If LITELLM_MASTER_KEY is set, it will be required for API access
# LITELLM_MASTER_KEY=sk-1234-your-master-key-here
# LITELLM_SALT_KEY=sk-salt-your-salt-key-here
# UI_USERNAME=admin
# UI_PASSWORD=password

# --- Database Storage ---
# Uncomment for persistent model and key storage (requires postgres)
# POSTGRES_PASSWORD=your-secure-password
# STORE_MODEL_IN_DB=true

# ==============================================================================
# OPTIONAL: Phoenix Session Tracking
# ==============================================================================
# Use these to track different workflows and prompt versions in Phoenix
# Managed automatically by the claude-session wrapper script

# Session metadata (set via claude-session script or manually)
# PHOENIX_SESSION_ID=code-review-20251101-143022-a3f2b1
# PHOENIX_SESSION_TYPE=code-review
# PHOENIX_PROMPT_VERSION=v2
# PHOENIX_SESSION_NAME=async-refactor

# ==============================================================================
# Usage:
# - Basic Setup: Set ANTHROPIC_API_KEY (for fallback non-OAuth auth)
# - Phoenix Setup: Use claude-session wrapper to track workflows
# - Arize Setup: Set ARIZE_API_KEY and ARIZE_SPACE_KEY for Arize observability
# - Advanced Setup: Also uncomment desired optional features, use docker-compose.yml --profile advanced
#
# Session Tracking:
#   Use the claude-session script to automatically manage session metadata:
#   ./claude-session --type code-review --version v2 --name "refactor-auth"
#   ./claude-session --type debugging --version v1 --shell
#   ./claude-session --reset
# ==============================================================================
